/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "w0.peakpx.com",
            "www.epitech-it.es",
            "wallpapers.com"
        ]

    }

};

export default nextConfig;